# Web框架
fastapi==0.136.1
uvicorn[standard]==0.46.0

# 数据库 (PostgreSQL)
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
psycopg2-binary>=2.9.0
alembic==1.18.4
pgvector>=0.4.2

# Redis
redis==5.3.1

# Celery任务队列
celery==5.6.2
flower==2.0.1

# 安全
PyJWT==2.13.0
bcrypt>=4.0.0
cryptography>=42.0.0
python-multipart==0.0.32

# 配置
pydantic==2.12.5
pydantic-settings==2.14.2

# API限流
slowapi==0.1.9

# 工具
python-dotenv==1.2.2
email-validator==2.3.0
python-dateutil>=2.8.0
idna==3.15
protobuf==6.33.5

# PDF导出
playwright==1.52.0
markdown>=3.10.2
jinja2>=3.1.0
pypdfium2>=5.13.0,<6

# 文档预览转换
python-pptx>=1.0.0
markitdown>=0.1.0
olefile>=0.47

# LLM (OpenAI 兼容协议)
openai>=1.0.0

# OCR (PaddleOCR 需要 PyTorch)
# PyTorch CPU版本 - 需单独安装:
# pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
# 注意: PaddlePaddle 3.3.0在Windows上存在oneDNN兼容性问题，需使用3.2.0
# CPU: pip install paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
# NVIDIA GPU (CUDA 12.6): pip install paddlepaddle-gpu==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# Dockerfile 会按部署环境选择其中一个 wheel；此处保留 CPU 依赖供本地默认安装。
paddlepaddle==3.2.0
paddleocr==3.5.0
paddlex[ocr]==3.5.2
pillow==12.3.0
numpy>=1.21,<2.0
opencv-python-headless>=4.8.0

# 测试
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
pytest-cov==5.0.0
aiosqlite==0.22.1
